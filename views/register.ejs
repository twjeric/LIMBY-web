<!DOCTYPE html>
<html>
  <head>
    <title>Login</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% if (error){ %>
        <div><%= error %></div>
    <% } %>
    <form action="/register" method="get">
        <div>
          Email (Particle account associated with your perch): <input type="text" name="email">
        </div>

        <div>
          Password (Particle account associated with your perch): <input type="password" name="password">
        </div>

        <button type="submit">Register</button>
    </form>

    <form action="/login" method="get">
      <button type="submit">I want to log in actually!</button>
    </form>
  </body>
</html>
